# ENV Var Confessional

A bash script that checks if your required environment variables exist and aren't empty. Stops configuration sins before they become debugging nightmares. Run: `./env_confessional.sh .env DATABASE_URL API_KEY SECRET_TOKEN`